<h1><%= @article.title %></h1>
<p><%= @article.body %></p>

<p align=right>
<%= link_to "Edit Article", edit_article_path(@article) %> </br>
<%= link_to "Delete Article", article_path(@article), method: :delete, data: {confirm: "Really want to delete?" }%></br>
</p>
<h3>Comments (<%= @article.comments.count %>)</h3>
<%= render partial: 'articles/comment', collection: @article.comments %>
<%= render partial: 'comments/form' %>

<p align=right> <%= link_to "View all articles", articles_path %> </p>